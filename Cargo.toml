[package]
name = "discordfs"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.7", features = ["derive"] }
fuser = "0.18.0"
rustyline = { version = "18.0.0", default-features = false, features = ["derive"] }
